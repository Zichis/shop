<div class="px-10 py-5">
    <p class="text-sm text-gray-500">{{ date('d F, Y') }}</p>
    <h1 class="text-3xl">{{ auth()->user()->name }}</h1>
    <p class="text-sm text-yellow-600 font-bold">T & R Super Store</p>
</div>
